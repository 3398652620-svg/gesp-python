# 七阶段知识依赖设计 V1

> 课程体系：GESP Python 一级 + Python 基础拓展 38 讲  
> 定位：课程研发第二层——在切分 38 讲课次之前，先确定七个阶段内部与阶段之间的知识依赖关系。  
> 说明：本设计不参考旧 OJ 系统中的旧版课次标题与旧题目归属，只基于当前已经冻结的课程总纲与课程理念进行课研设计。

---

# 一、设计目标

这一层暂时不急着把课程硬切成 38 个标题。

当前只回答一个核心问题：

> **一个孩子从刚开始接触 Python，到能够完成 GESP 一级，再到具备列表、函数、二维列表这些真正的 Python 基础，中间的认知应该怎样自然生长？**

这一层重点检查三件事：

1. 知识依赖是否成立；
2. 程序思维训练是否连续；
3. 阶段之间是否存在断层。

只有七阶段的内部知识链全部稳定后，才进入下一层：

> **将完整知识结构合理切分为 38 个教学单元。**

38 讲是当前课程容量，不应反过来决定知识结构。

---

# 二、第一阶段：建立“程序、数据、状态”的最小模型

## 1. 阶段入口

默认学生：

- 可能接触过图形化，也可能完全没有；
- 不要求懂 Python；
- 不要求懂变量；
- 不要求懂编程术语。

因此第一阶段不能一开始就只关注“做程序”。

首先应建立一个最朴素的认识：

> **程序就是按照明确规则执行的一系列指令。**

再逐渐进入：

> 指令可以处理数据；  
> 数据可以被保存；  
> 保存的数据可以发生变化。

---

## 2. 本阶段最核心的三个认知

### 2.1 程序有执行顺序

```python
print("A")
print("B")
```

为什么先输出 A，再输出 B？

这是以后所有：

- 分支；
- 循环；
- 函数；

的地基。

学生必须从一开始建立：

> **程序按照真实执行顺序运行。**

---

### 2.2 程序可以保存状态

```python
x = 10
```

不能只停留在：

> “变量像一个盒子。”

更重要的是理解：

> **现在程序需要记住一个值，所以给它一个名字。**

变量从第一次出现时，就应该逐渐承担“状态”的意义。

---

### 2.3 状态可以变化

```python
x = 10
x = 20
```

这里需要埋下整套课程最重要的一颗种子：

> **变量的值不是永远不变的。**

以后：

```python
total += i
count += 1
height += A
```

本质上全部是这一认知的延伸。

---

## 3. 第一阶段知识依赖顺序

> **程序与执行**  
> ↓  
> **输出**  
> ↓  
> **数据与数据类型初识**  
> ↓  
> **变量与赋值**  
> ↓  
> **变量变化**  
> ↓  
> **表达式与运算**  
> ↓  
> **表达式执行顺序**  
> ↓  
> **变量 + 表达式 + 输出综合**

---

## 4. 数据类型的处理原则

第一阶段需要知道：

```python
10
3.14
"hello"
```

不是同一种数据。

但不应一开始把：

> int / float / str / bool

做成“四种类型定义背诵”。

尤其 `bool`，真正具有认知意义的时机是在条件阶段。

因此采用：

> **先建立“数据有类型”，后续再逐步精确。**

---

## 5. 第一阶段题型成长

前期：

> 看代码 → 猜输出

逐渐进入：

> 变量变化 → 跟踪结果

最后：

> 给一个固定数据的小问题 → 自己写表达式 → 输出结果

所以第一阶段真正训练的不是“会写 print”，而是：

> **第一次按程序执行顺序思考。**

---

## 6. 第一阶段能力出口

学生至少能够：

- 看懂短小顺序程序；
- 跟踪变量值；
- 理解重新赋值；
- 写基础表达式；
- 理解运算顺序；
- 用变量保存并计算数据；
- 预测程序最终输出。

### 阶段测评 T1

重点验证：

> **程序阅读 + 变量跟踪 + 基础表达式。**

不是语法单词默写。

---

# 三、第二阶段：从“程序里的数据”进入“外部问题”

第一阶段的数据主要由程序员提前写入。

第二阶段自然产生新的问题：

> **如果运行之前不知道数据怎么办？**

这个问题第一次让程序从“固定代码演示”变成：

> **可以处理不同输入的问题求解工具。**

---

## 1. 核心认知一：输入得到的是文本信息

```python
x = input()
```

学生即使输入：

```text
10
```

程序最初得到的仍然是文本信息。

因此才自然需要：

```python
int()
float()
```

这样“类型转换”不是一个孤立语法，而是因为真实需求出现。

---

## 2. 核心认知二：现实问题可以拆成程序步骤

例如：

> 输入长和宽，求长方形面积。

真正训练的不是乘法，而是：

> 输入什么？  
> 哪些值需要保存？  
> 怎样计算？  
> 最后输出什么？

这是问题建模第一次正式出现。

---

## 3. 第二阶段知识依赖顺序

> **input**  
> ↓  
> **input 的字符串本质**  
> ↓  
> **类型转换**  
> ↓  
> **输入多个数据**  
> ↓  
> **计算**  
> ↓  
> **格式化输出**  
> ↓  
> **完整顺序程序**

---

## 4. 格式化输出的教学时机

格式化输出应该在学生已经出现：

> “结果虽然能输出，但表达得不够清楚”

的需求以后进入。

不能因为 Python 有 f-string，就在课程过早阶段一次性讲完所有格式规则。

继续坚持：

> **需求产生知识。**

---

## 5. 第二阶段程序思维训练

这一阶段重点从：

> “这一行执行完变量变成什么？”

升级为：

> **完成一个问题需要哪些步骤？**

程序思维开始出现第一层“流程感”。

---

## 6. 第二阶段能力出口

面对一个简单计算任务，学生能够自己完成：

> **输入 → 保存 → 转换 → 计算 → 输出**

而不是老师把五行程序写四行。

### 阶段测评 T2

给学生一个新的简单生活计算问题。

不要完全复刻课堂例子。

重点验证：

> **学生能不能自己把自然语言翻译成顺序程序。**

---

# 四、第三阶段：从“计算”进入“选择”

到这里，学生已经知道：

> 程序可以接收数据并计算。

现在需要制造新的认知需求：

> **如果不同数据应该得到不同处理怎么办？**

于是分支结构出现。

---

## 1. 第三阶段核心知识链

> **比较**  
> ↓  
> **产生真假**  
> ↓  
> **真假决定是否执行**  
> ↓  
> **两种情况**  
> ↓  
> **多种情况**  
> ↓  
> **一个条件不够**  
> ↓  
> **组合条件**

---

## 2. 布尔值应在这里真正建立

前面可以知道“数据有不同类型”。

但：

```python
True
False
```

真正有意义，是学生看到：

```python
10 > 5
```

这个表达式本身也会产生一个结果。

再连接：

```python
if 10 > 5:
```

学生才能真正理解：

> **if 实际上依赖的是条件最终是真还是假。**

比直接背：

> “布尔类型有 True 和 False”

更自然。

---

## 3. 分支阶段真正训练的是“条件翻译”

重点不是只会写 `if`。

而是：

> **把自然语言规则翻译成程序条件。**

例如：

> 年龄满 18 岁并且持有有效证件。

学生应该逐渐学会拆分：

> 条件 A 是什么？  
> 条件 B 是什么？  
> 是同时满足，还是满足其中一个？

这才是逻辑训练。

---

## 4. 取模应在这里被重新激活

`%` 在前面的运算阶段已经出现。

当时只是：

> 求余数。

现在它第一次成为真正的问题解决工具：

- 奇偶；
- 整除；
- 周期。

这是一种重要的旧知识复用。

知识不能学完以后死亡。

---

## 5. 第三阶段题型成长

先：

> 条件是真是假？

再：

> 程序会走哪一条分支？

再：

> 自己写一个判断。

最后：

> 自然语言规则 → 设计完整条件。

这一阶段应大量处理：

- 边界值；
- 条件覆盖；
- 判断顺序。

防止学生出现：

> 语法会写，但逻辑边界没想明白。

---

## 6. 第三阶段能力出口

学生能够：

- 从问题里识别“需要分情况”；
- 写真假条件；
- 组织单分支、双分支、多分支；
- 表达复合规则；
- 阅读代码并判断执行路径；
- 对边界条件产生意识。

### 阶段测评 T3

不能只测简单逻辑表达式。

至少应真正出现：

> **把一段自然语言规则翻译成程序。**

否则测不到阶段能力。

---

# 五、第四阶段：从“执行一次”进入“状态反复变化”

这是整个课程最重要的转折。

前面的程序主要是：

> 输入一次，算一次，判断一次。

循环阶段开始进入：

> **一个状态在很多次执行中不断变化。**

因此这一阶段是整套课程程序思维训练最密集的部分。

---

## 1. 循环学习的三个认知层次

### 第一层：重复本身

例如：

> 打印 5 次 Hello。

先理解：

> 一段代码可以重复发生。

---

### 第二层：每一轮并不完全一样

引入循环变量：

```python
for i in range(5):
```

每轮：

```text
i = 0
i = 1
i = 2
...
```

学生第一次真正理解：

> **循环不是复制粘贴，每一轮可以拥有不同状态。**

---

### 第三层：循环结果需要被保存

然后自然产生：

```python
total
count
```

这个顺序非常重要。

如果刚学 for 就直接要求学生套：

```python
total = 0
for ...
```

学生很容易把它当模板。

应该先建立：

> 每一轮都有自己的值。

再追问：

> **前几轮得到的东西怎么办？**

累加器、计数器才真正有存在理由。

---

# 六、第四阶段核心知识依赖主链

> **for 与重复**  
> ↓  
> **range 与循环变量**  
> ↓  
> **循环中的状态变化**  
> ↓  
> **累加**  
> ↓  
> **计数**  
> ↓  
> **条件 + 循环组合**  
> ↓  
> **枚举与筛选**  
> ↓  
> **break / continue**  
> ↓  
> **while 与条件型重复**  
> ↓  
> **状态模拟**  
> ↓  
> **数字拆解**  
> ↓  
> **数字重构**  
> ↓  
> **嵌套循环**  
> ↓  
> **综合算法**

当前这里只确认知识依赖关系。

不代表一个箭头必须对应一讲。

后续再根据 80 分钟真实教学负荷切分课次。

---

# 七、为什么累加与计数值得重点训练

这不是“小算法”。

这是学生第一次真正学习：

> **程序如何产生一个逐渐成长的答案。**

例如：

```python
total = 0
```

需要真正理解：

> 为什么它在循环外？  
> 它每一轮保存的是什么？  
> 它什么时候更新？

如果学生真正理解了这些问题，实际上已经开始接触：

> **状态生命周期。**

---

# 八、枚举思想为什么必须认真教

很多一级基础算法的本质都是：

> **不知道答案在哪里，就把可能情况一个一个试。**

例如：

- 找因数；
- 判断质数；
- 找符合条件的数；
- 简单穷举。

这已经是非常真实的算法思维。

不复杂，但非常基础。

---

# 九、for 与 while 必须解决不同认知问题

不能只教成：

> 两种循环，语法不同。

应该形成明确区别。

### for

> **我要依次处理一个明确范围或明确序列。**

### while

> **我不知道要执行多少轮，但知道继续执行的条件。**

否则学生很容易永远不知道为什么 Python 需要两种循环。

---

# 十、状态模拟必须占有独立地位

像：

> 《蚂蚁爬树》

这样的任务非常有价值。

因为它会把：

- 时间；
- 位置；
- 事件顺序；
- 条件；
- break；

自然组合起来。

它不是“项目课”。

更准确地说，它是：

> **程序模拟现实过程。**

这类任务可以成为课程中非常重要的自然命名成果。

---

# 十一、数位问题为什么应放在 while 之后

```python
n % 10
n // 10
```

天然构成：

> 当前数字 → 取一位 → 处理 → 去掉一位 → 继续

这就是一个不断改变状态直到结束的 while 模型。

因此数位问题与 while 的知识衔接非常自然。

---

# 十二、嵌套循环为什么需要较晚出现

嵌套循环不应该只理解成：

> “for 里面再写一个 for。”

真正理解它，需要学生已经稳定建立：

> **一个完整重复过程中，又包含另一个完整重复过程。**

如果单层循环的状态模型还没形成，嵌套循环很容易退化成代码形状记忆。

---

# 十三、第四阶段能力出口

学生应该开始真正拥有：

> **从问题里识别状态的能力。**

面对一道新问题会主动问：

> 哪部分重复？  
> 我需要记什么？  
> 初始状态是什么？  
> 每一轮改变什么？  
> 什么时候停止？

如果这个阶段建立成功，整套课程最重要的地基就站住了。

### 阶段测评 T4

从这里开始，应明显降低纯语法识别题的比例。

测试必须真正验证：

> **状态设计能力。**

---

# 十四、第五阶段：把程序状态“看见”

Turtle 不能成为：

> “学了二十几节代码后奖励三节画画。”

这样会割裂整套课程。

它真正的价值应当是：

> **把程序执行产生的状态变化变成视觉轨迹。**

---

## 1. Turtle 本身就是状态模型

海龟天然拥有：

- 当前位置；
- 当前方向；
- 当前画笔状态；
- 当前颜色。

执行：

```python
forward(100)
```

状态发生变化。

执行：

```python
left(90)
```

状态再次变化。

这和：

```python
height += A
```

在程序思想上是一致的。

一个是数值状态，一个是可视位置状态。

---

## 2. Turtle 内部知识依赖

> **位置与方向**  
> ↓  
> **基本运动**  
> ↓  
> **重复运动**  
> ↓  
> **几何规律**  
> ↓  
> **参数变化**  
> ↓  
> **坐标**  
> ↓  
> **画笔状态**  
> ↓  
> **组合图形**  
> ↓  
> **规律作品**

Turtle 不是重新学习一套语法。

而是：

> **让旧语法进入一个非常适合儿童观察程序规律的环境。**

---

## 3. 第五阶段能力出口

学生看到一个有规律的图案，能够开始思考：

> 哪部分重复？  
> 每轮角度怎么变？  
> 哪个参数决定大小？  
> 怎么用循环表达？

### 阶段测评 T5

OJ 系统可以重点测：

- 代码阅读；
- 运动轨迹判断；
- 循环规律；
- 位置与方向变化。

实际 Turtle 作品本身承担另一部分能力证据。

不必强行让 OJ 系统承载所有视觉作品。

---

# 十五、第六阶段：把“会编程”转换成“稳定完成一级任务”

29～32 所在阶段应明确一个边界：

> **基本停止扩张知识版图。**

如果所谓冲刺阶段还在不断增加大量新知识，说明前面课程没有真正闭合。

这一阶段解决的是：

> 我已经学过，但为什么完整题目或考试里仍然会错？

---

## 1. 第六阶段能力转换链

> **零散知识**  
> ↓  
> **识别题目在考什么**  
> ↓  
> **按执行顺序阅读程序**  
> ↓  
> **完整解决编程问题**  
> ↓  
> **在规定时间内稳定完成**  
> ↓  
> **知道自己为什么错**

---

## 2. 本阶段核心训练

### 客观题能力

不是看到选项凭感觉。

### 代码阅读能力

建立稳定 Trace 习惯。

### 编程题完整性

很多学生不是算法完全不会，而是：

- 漏输入；
- 类型错误；
- 边界错误；
- 输出错误；
- 程序没写完。

因此需要专门训练：

> **完整完成一个程序。**

### 考试执行能力

包括时间分配与检查。

### 复盘能力

把：

> “我这题错了。”

升级成：

> “我是因为循环次数跟错 / 边界没检查 / 知识缺口 / 读题错误。”

---

## 3. 第六阶段能力出口

不仅是：

> 达到一级及格线。

而应该是：

> **一级范围知识和问题解决方式已经形成闭环。**

### 计算机基础常识依赖设计

GESP 一级考查的基础计算机常识（CPU、内存、I/O 设备、操作系统、文件系统、网络基础等），不在第 01 讲集中灌输，而是采用**第一至第五阶段轻量分布式渗透 + 第 29 讲集中复习**的方式推进，确保知识自然习得且不干扰编程主线。

### 阶段测评 T6

由独立设置的 **120 分钟 GESP Python 一级全真模拟** 承担（不占用 38 讲课次，直接复用 OJ / 刷题系统）。

第 31 讲负责综合限时实战与模拟前训练，第 32 讲基于 T6 真实模拟结果进行深度复盘与错误归因。

---

# 十六、第七阶段：从“一个答案”进入“数据与代码组织”

32 讲一级阶段结束后，学生已经具备基本程序控制与基础算法能力。

这时再进入：

- 列表；
- 函数；
- 二维列表；

认知意义会更加自然。

因为学生已经可以真实感受到旧方法的局限。

---

# 十七、第一条拓展主线：数据组织

## 1. 为什么列表此时出现

以前数据较少：

```python
a = 10
b = 20
c = 30
```

问题不大。

但如果：

> 有 30 个数据怎么办？

列表的出现就有真实需求。

---

## 2. 列表不能止于语法操作

如果只教：

- 创建；
- 下标；
- append；
- len；

仍然只是“列表语法课”。

列表真正改变的是：

> **循环终于可以处理现实中的一批数据。**

于是之前的算法思想获得第二次生命：

- 累加 → 列表求和；
- 计数 → 条件计数；
- 枚举 → 遍历；
- 查找 → 数据查找；
- 当前答案 → 最大值 / 最小值。

因此：

> **列表基础 → 列表算法**

必须紧密相连。

---

# 十八、第二条拓展主线：代码组织

随着程序变长，学生会真实经历：

> 同一段代码又写了一遍。

函数这时自然出现。

不是因为：

> “Python 基础应该学函数。”

而是因为：

> **程序越来越长，我们需要把功能组织起来。**

---

## 1. 函数认知应分为两层

第一层：

> 一段代码可以有名字，并被反复调用。

第二层：

> 函数不仅可以执行事情，还可以产生结果。

因此合理知识链：

> **函数基础**  
> ↓  
> **参数**  
> ↓  
> **return**  
> ↓  
> **算法封装**

`print` 与 `return` 必须成为这一阶段真正需要解决的认知冲突。

---

# 十九、第三条拓展主线：二维数据

学生已经掌握：

> 一批数据。

下一步自然产生：

> **如果数据本身有行有列怎么办？**

二维列表出现。

这里可以再次回收旧知识：

> 以前嵌套循环用来生成九九乘法表、图形规律。

现在：

> **嵌套循环开始真正处理二维数据。**

这是课程希望形成的状态：

> 新知识不是凭空出现，旧知识不断升级身份。

---

# 二十、第七阶段能力出口

最终学生应完成一次：

> **数据 + 循环 + 条件 + 函数 + 二维数据**

的整合。

不要求大型软件。

但必须第一次明显感受到：

> **程序开始有结构了。**

### 阶段测评 T7

重点验证：

> **学生能不能组织一个稍完整程序。**

而不是只问：

> `append()` 是什么意思？

---

# 二十一、七阶段总知识依赖

| 阶段 | 学生脑子里新增的核心问题 |
|---|---|
| 第一阶段 | **程序怎样保存和改变数据？** |
| 第二阶段 | **程序怎样处理外部输入并完成一个过程？** |
| 第三阶段 | **不同情况应该执行什么？** |
| 第四阶段 | **重复过程中需要维护什么状态？答案怎样逐步形成？** |
| 第五阶段 | **程序的状态变化能不能被直接看见？** |
| 第六阶段 | **已经学会的能力怎样稳定用于完整问题与考试？** |
| 第七阶段 | **数据和代码越来越多以后，怎样组织？** |

整条链条是：

> 程序与状态  
> → 外部数据  
> → 条件路径  
> → 重复与状态维护  
> → 可视化程序过程  
> → 稳定完整解决问题  
> → 数据与代码组织

---

# 二十二、当前确认的三条纵向课研原则

## 原则一：变量从第一次出现就逐渐承担“状态”的意义

不能前期把变量只讲成“盒子”，到循环阶段突然出现：

> 累加变量、计数变量、当前最大值。

它们必须是一条连续的认知主线。

---

## 原则二：算法必须不断复用，而不是“一课一算法”

例如累加思想：

> 循环求和  
> → 数位和  
> → 列表求和  
> → 二维每行求和

学生不断看到同一个思想在不同场景中出现，才能真正发现：

> **原来这是一个可迁移的方法。**

计数、查找、最值等也遵循同一原则。

---

## 原则三：后续课程不断“回收旧知识”

知识学完不能死亡。

例如：

### `%`

> 运算符  
> → 奇偶  
> → 整除  
> → 因数  
> → 数位

### 循环

> 重复输出  
> → 累加  
> → 枚举  
> → 数位  
> → Turtle  
> → 列表  
> → 二维数据

### 嵌套循环

> 行列规律  
> → 九九乘法表  
> → Turtle 图案  
> → 二维列表

这种螺旋式复现是课程真正扎实的重要保证。

---

# 二十三、阶段测试原则

每个阶段设置一次能力测评，直接复用现有 OJ / 刷题系统。

阶段测试：

- 不作为新增课程知识；
- 不额外占用课程知识主线；
- 重点验证阶段能力出口；
- 本阶段知识为主，同时允许少量前置能力复现；
- 不做简单知识点默写；
- 尽量验证程序阅读、状态跟踪、问题建模与独立编程。

测试节点：

- T1：第一阶段结束；
- T2：第二阶段结束；
- T3：第三阶段结束；
- T4：第四阶段结束；
- T5：第五阶段结束；
- T6：120 分钟 GESP Python 一级独立全真模拟（第 31 讲后进行，第 32 讲复盘）；
- T7：第七阶段结束。

---

# 二十四、本层研发结论

当前七阶段知识依赖设计整体成立。

下一步进入：

> **38 讲课次切分与课次地图设计。**

下一阶段不是简单给 38 节课起名字。

而是根据：

- 认知跨度；
- 新知识数量；
- 80 分钟真实课堂承载量；
- 前置知识；
- 练习需求；
- 状态思维训练；
- 类题递进；
- 旧知识回收；

判断：

> **哪些认知变化适合放在同一讲，哪些必须拆开。**

最终形成正式的：

# 《38 讲课程课次地图 V1》

每讲至少明确：

- 本讲定位；
- 前置知识；
- 核心知识；
- 课程衔接；
- 知识点内部衔接；
- 类题衔接；
- 核心难点；
- 能力出口；
- 自然显性成果；
- 知识边界；
- 下一讲接口。

---

# 最终课研判断

> **Python 本身和程序思维始终是课程主线。**

> **状态是什么、状态什么时候变化，是整套课程最重要的纵向思维线之一。**

> **新知识应尽可能从旧知识的局限中自然产生，旧知识应在后续课程中反复升级和迁移。**

> **先保证知识结构正确，再切课次；先保证学生真正理解，再考虑课件如何表现。**
