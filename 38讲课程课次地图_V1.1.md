# GESP Python 一级 + Python 基础拓展 38 讲课程课次地图 V1.1

> 版本定位：课程研发第三层（V1.1 依据官方核查补丁增量修订）  
> 上位文件：
> - 《GESP Python 一级 + Python 基础拓展 38 讲课程总纲 V3.1》
> - 《七阶段知识依赖设计 V1》
>
> 本文件任务：在已经确定的课程理念与七阶段知识依赖基础上，将完整知识结构合理切分为 38 个教学单元。
>
> 本文件**不是课件脚本**，也不规定“老师怎么讲”。  
> 它只负责锁定每一讲的：
>
> - 本讲定位；
> - 前置知识；
> - 核心知识；
> - 课程衔接；
> - 知识点内部衔接；
> - 类题衔接；
> - 核心难点；
> - 能力出口；
> - 自然显性成果；
> - 知识边界；
> - 下一讲接口。
>
> 后续 Gemini 可依据本地图 + 单讲课研规格，自主完成教学设计与 HTML 课件实现。

---

# 一、课次切分原则

38 讲不是为了凑数字而切。

每一讲是否独立成课，主要依据：

1. **认知跨度**：这一讲是否包含一个相对完整的认知变化；
2. **新知识数量**：80 分钟内能否完成“理解 + 练习 + 迁移”；
3. **程序思维负荷**：是否需要大量 Trace、变量跟踪、状态推演；
4. **类题容量**：是否有足够的基础题、同构题、变式题形成闭环；
5. **前置依赖**：是否已经具备必要旧知识；
6. **旧知识回收**：是否能自然复用已有能力；
7. **知识边界**：是否会为了完成任务而偷跑后续语法。

整套课始终遵循：

> **知识决定主线，问题驱动思考，任务承载练习，项目负责整合。**

---

# 二、38 讲总览

| 课次 | 课研标题 | 主要认知推进 |
|---|---|---|
| 01 | 程序与执行：计算机为什么能“听懂”代码 | 建立程序、指令、执行顺序的最小模型 |
| 02 | 输出与基本数据：让程序表达信息 | `print()`、字符串、数值、顺序输出 |
| 03 | 变量与赋值：让程序记住数据 | 变量、命名规则（字符/开头/大小写/关键字/意义）、赋值、重新赋值 |
| 04 | 变量变化与程序跟踪：状态是怎样变化的 | 连续赋值、表达式赋值、Trace |
| 05 | 算术运算与表达式：让数据参与计算 | `+ - * / // %` 与基本表达式 |
| 06 | 复杂表达式与运算顺序 | `**`、括号、优先级、复杂表达式阅读 |
| 07 | 变量、表达式与输出综合 | 基础程序综合、简单 f-string 表达 |
| 08 | `input()`：程序怎样获得外部数据 | 输入、字符串本质、输入保存 |
| 09 | 类型转换：把输入变成可计算的数据 | `int()`、`float()`、`str()` |
| 10 | 顺序建模：把现实计算翻译成程序 | 多数据输入、变量设计、公式拆解 |
| 11 | 输出组织与顺序程序综合 | 清晰输出、常见输出控制（`end=""`等）、格式化表达、完整顺序任务 |
| 12 | 比较与布尔：条件为什么会有真假 | 比较运算、True/False、条件结果 |
| 13 | `if` 与 `if/else`：让程序开始做决定 | 单分支、双分支、缩进、路径 |
| 14 | 取模与周期条件：把 `%` 变成判断工具 | 奇偶、整除、周期问题 |
| 15 | `elif` 与区间判断：处理多种互斥情况 | 多分支、判断顺序、区间 |
| 16 | 复合条件与分支综合 | `and/or/not`、简单嵌套、规则翻译 |
| 17 | `for` 与 `range`：描述明确范围的重复 | for、range、循环变量 |
| 18 | 累加器与计数器：让循环“记住”结果 | 累加、计数、状态初始化与更新 |
| 19 | 条件 + 循环：枚举、筛选与统计 | 循环中判断、枚举、条件筛选 |
| 20 | 因数与质数：把枚举变成算法 | 因数、质数、状态记录 |
| 21 | `break` 与 `continue`：改变循环执行路径 | 跳过本轮、提前结束 |
| 22 | `while` 与状态模拟：蚂蚁爬树 | 条件型重复、状态推进、停止条件 |
| 23 | 数位拆解：逐位处理一个整数 | `% 10`、`// 10`、数位统计 |
| 24 | 数字重构：逆序、回文与特殊数 | 重构状态、回文、综合数位算法 |
| 25 | 嵌套循环与循环综合 | 双层重复、行列关系、九九乘法表、综合 |
| 26 | Turtle 状态：位置、方向与基本运动 | 海龟状态、基本运动（import/forward/backward/left/right/默认状态等） |
| 27 | 循环绘图：用程序表达几何规律 | 正多边形、circle(r, steps)、pencolor/pensize、参数与循环 |
| 28 | 坐标与综合绘图：把规则组合成作品 | goto/penup/pendown/填充/write/done/综合作品 |
| 29 | GESP 客观题与代码阅读专项 | 稳定 Trace、表达式/分支/循环/Turtle 阅读、计算机基础常识集中复习 |
| 30 | GESP 编程题完整解题专项 | 读题、建模、编码、自测、边界、输出 |
| 31 | GESP 一级综合限时实战 / 全真模拟前训练 | 混合题限时实战、作答节奏、时间分配策略、考前操作检查 |
| 32 | 模拟复盘与一级能力收束 | 基于 T6 120分钟全真模拟结果的错误归因、查缺补漏、能力画像 |
| 33 | 列表基础：一次管理一组数据 | 创建、索引、修改、append、len、遍历 |
| 34 | 列表算法：让循环处理真实数据 | 求和、计数、最值、查找、下标 |
| 35 | 函数基础：给一段代码起名字 | def、调用、执行流程、参数 |
| 36 | `return` 与算法封装：让结果带回来 | 返回值、print vs return、旧算法封装 |
| 37 | 二维列表：数据也可以有行和列 | 双下标、修改、嵌套遍历 |
| 38 | 二维数据综合：把知识组织成完整程序 | 列表 + 条件 + 循环 + 函数 + 二维数据 |

---

# 三、第一阶段｜01～07 程序、数据与状态

## 第01讲 程序与执行：计算机为什么能“听懂”代码

### 本讲定位
建立整套课程的最小程序模型。

本讲不是计算机常识百科，而是让学生第一次理解：

> **程序是一组明确指令，计算机会按照规则和顺序执行。**

### 前置知识
无。

### 核心知识
- 计算机、程序、代码的基本关系；
- 编程语言的作用；
- Python 是一种编程语言；
- 程序需要执行；
- 指令有先后顺序；
- 编辑、运行、观察结果的基本流程。

### 课程衔接
这是全课程入口。

下一讲将从：

> “程序会执行指令”

进入：

> “程序怎样把执行结果表达出来？”

### 知识点内部衔接
程序是什么  
→ 为什么需要编程语言  
→ Python 是什么  
→ 代码如何运行  
→ 程序按顺序执行。

### 类题衔接
本讲不做复杂编程题。

适合：
- 判断“哪一个是程序/代码/输出”；
- 给出两行简单指令，预测执行先后；
- 简单运行与修改体验。

### 核心难点
避免把本讲变成大量硬件、操作系统、网络知识的百科课。

### 能力出口
学生能够说清：
- 代码是给计算机执行的指令；
- 程序不是图片或效果本身；
- 代码存在执行顺序；
- 能完成一次最基本的“写入 → 运行 → 看结果”。

### 自然显性成果
**无，不强行包装。**

可以把“成功运行第一段 Python 代码”作为课堂成就，但不包装成项目。

### 知识边界
不正式教授：
- 变量；
- 输入；
- 条件；
- 循环；
- 函数。

### 下一讲接口
既然程序能执行，那么：

> **怎样让程序把信息展示出来？**

---

## 第02讲 输出与基本数据：让程序表达信息

### 本讲定位
建立程序输出与“数据长什么样”的最初认识。

### 前置知识
- 程序；
- 执行顺序；
- 基本运行流程。

### 核心知识
- `print()`；
- 字符串；
- 数字字面量；
- 引号；
- 多条 `print()` 的执行顺序；
- 简单输出错误识别。

### 课程衔接
01 讲建立“程序会执行”。

02 讲解决：

> **执行以后，程序怎样把信息给人看？**

下一讲进一步提出：

> 如果一个数据以后还要继续使用，难道每次都重新写吗？

### 知识点内部衔接
输出固定文字  
→ 输出数字  
→ 字符串与数字形式不同  
→ 多行代码按顺序输出。

### 类题衔接
基础：
- 预测单个 `print()` 输出；
- 判断引号是否正确。

同构：
- 多个 `print()` 顺序输出。

变式：
- 字符串与数值混合辨析。

### 核心难点
学生容易把：
- `10`
- `"10"`

当作完全相同的东西。

这里先建立“它们不是同一种数据”的直觉，不急于展开完整类型体系。

### 能力出口
学生能够：
- 正确使用 `print()`；
- 区分最基础的字符串和数字形式；
- 根据代码顺序预测输出。

### 自然显性成果
可自然形成：

> **《我的第一张信息卡》**

仅在课堂本身已经需要多行输出时使用，不额外增加未来知识。

### 知识边界
不正式教授：
- 变量；
- 类型转换；
- f-string；
- input。

### 下一讲接口
固定数据一直写在 `print()` 里很不方便。

下一讲解决：

> **程序怎么把数据记住？**

---

## 第03讲 变量与赋值：让程序记住数据

### 本讲定位
第一次正式建立“状态”意识。

### 前置知识
- 字符串、数字的初步区别；
- 顺序执行；
- `print()`。

### 核心知识
- 变量与赋值；
- 变量名与合法命名规则：
  - 由字母、数字、下划线组成；
  - 变量名不能以数字开头；
  - Python 大小写敏感；
  - Python 关键字（保留字）不能作为普通变量名；
  - 有意义的变量命名；
  - 常量命名约定轻量认识；
- `=` 赋值与重新赋值；
- 用变量输出当前状态。

### 课程衔接
02 讲的数据都是“写死”的。

03 讲解决：

> **程序需要以后继续使用某个数据时怎么办？**

### 知识点内部衔接
固定值  
→ 数据需要保存  
→ 给数据一个名字  
→ 变量  
→ 变量可以重新获得新值。

### 类题衔接
基础：
- 某变量当前值是多少；
- `print(x)` 输出什么。

同构：
- 多个变量保存不同数据。

变式：
- 变量被重新赋值后，旧值是否仍然是当前状态。

### 核心难点
1. `=` 不是数学中的“左右永远相等”；
2. 变量不是固定盒子，当前值会变化。

### 能力出口
学生能够：
- 定义变量；
- 正确赋值；
- 重新赋值；
- 说出变量在某一时刻的当前值。

### 自然显性成果
**无，不强行包装。**

### 知识边界
暂不深入：
- `x = x + 1` 的循环意义；
- 复合赋值；
- 输入。

### 下一讲接口
变量可以改变。

下一讲进一步追问：

> **如果一个变量的新值依赖它原来的值，会发生什么？**

---

## 第04讲 变量变化与程序跟踪：状态是怎样变化的

### 本讲定位
显式建立“程序状态跟踪”能力。

这是后续分支、循环、累加器、函数执行流的重要地基。

### 前置知识
- 变量；
- 赋值；
- 重新赋值；
- 顺序执行。

### 核心知识
- 连续赋值；
- 变量参与简单表达式；
- `x = x + 1`；
- 变量交换/更新的简单阅读；
- Trace Table 初步；
- 当前值与旧值的区别。

### 课程衔接
03 讲知道变量“能变”。

04 讲开始真正研究：

> **它是怎样一步一步变的？**

### 知识点内部衔接
当前值  
→ 用当前值算新值  
→ 新值覆盖旧值  
→ 按执行顺序逐行跟踪。

### 类题衔接
基础：
- 连续三次赋值后结果。

同构：
- `x = x + 2`、`x = x * 2`。

变式：
- 多变量互相引用；
- 找出哪一步理解错误。

### 核心难点
学生最容易凭直觉“跳着看代码”。

本讲必须建立：

> **每执行一行，状态更新一次。**

### 能力出口
学生能够使用简单 Trace 表逐行跟踪变量。

### 自然显性成果
**无，不强行包装。**

### 知识边界
不使用：
- 循环；
- 条件；
- 输入。

### 下一讲接口
变量已经能保存和改变。

接下来需要解决：

> **程序究竟能怎样计算这些数据？**

---

## 第05讲 算术运算与表达式：让数据参与计算

### 本讲定位
建立 Python 中常用数值运算的意义。

### 前置知识
- 变量；
- 状态更新；
- 基础数字。

### 核心知识
- `+`
- `-`
- `*`
- `/`
- `//`
- `%`
- 基础表达式；
- 变量参与运算。

### 课程衔接
04 讲已经出现简单表达式更新。

05 讲正式系统化：

> **程序可以如何计算数据？**

### 知识点内部衔接
日常加减乘除  
→ Python 运算符  
→ `/` 与 `//`  
→ `%` 的余数意义  
→ 变量进入表达式。

### 类题衔接
基础：
- 单一运算。

同构：
- 用变量代替数字。

变式：
- `/`、`//`、`%` 对比；
- “商”和“余数”场景。

### 核心难点
- `/` 与 `//`；
- `%` 的实际意义。

这里先教“余数”，后续在分支、因数、数位处理中不断回收。

### 能力出口
学生能够把简单数学关系写成 Python 表达式。

### 自然显性成果
**无，不强行包装。**

### 知识边界
`%` 暂时不深入：
- 奇偶算法；
- 因数；
- 数位。

这些留到后续自然回收。

### 下一讲接口
表达式一复杂，就会出现：

> **到底先算谁？**

---

## 第06讲 复杂表达式与运算顺序

### 本讲定位
建立“程序不是凭感觉算表达式”的规则意识。

### 前置知识
- 常见算术运算符；
- 变量；
- 基础表达式。

### 核心知识
- `**`；
- 括号；
- 运算优先级；
- 同级运算顺序；
- 复杂表达式跟踪。

### 课程衔接
05 讲解决“能怎么算”。

06 讲解决：

> **多个运算同时出现时，程序按什么顺序算？**

### 知识点内部衔接
单一运算  
→ 多运算组合  
→ 产生顺序冲突  
→ 优先级与括号。

### 类题衔接
基础：
- 两种运算组合。

同构：
- 括号改变顺序。

变式：
- `//`、`%`、`**` 混合；
- 判断两个表达式是否等价。

### 核心难点
不是背一长串优先级口诀，而是建立：
- 括号优先；
- 常见运算层级；
- 不确定时用括号表达意图。

### 能力出口
学生能够正确预测常见复杂表达式结果。

### 自然显性成果
**无，不强行包装。**

### 知识边界
幂运算只要求理解常见正整数指数。

可轻量说明：
> 当指数是正整数时，可以理解为重复相乘。

不深入负指数、根式等。

### 下一讲接口
现在学生已经有：
- 数据；
- 变量；
- 表达式；
- 输出。

下一讲第一次把这些组合成“小程序”。

---

## 第07讲 变量、表达式与输出综合

### 本讲定位
第一阶段收束。

把前面零散语言元素第一次组合成完整的小型顺序程序。

### 前置知识
01～06 全部基础知识。

### 核心知识
- 变量组织；
- 表达式计算；
- 多步状态变化；
- 输出结果；
- 简单 f-string，用于自然表达变量结果；
- 基础程序阅读与独立书写。

### 课程衔接
这是第一阶段第一次真正回答：

> **我已经能用 Python 做什么？**

下一阶段将引入运行时输入。

### 知识点内部衔接
变量保存  
→ 表达式处理  
→ 结果变化  
→ 输出表达。

### 类题衔接
基础：
- 短代码 Trace。

同构：
- 改数据重新预测。

变式：
- 找错；
- 补空；
- 根据要求写短程序。

综合：
- 固定数据的小型计算报告。

### 核心难点
学生是否真的能脱离单一语法题，把几种知识组合起来。

### 能力出口
学生能够独立完成一个：
> 固定数据 → 计算 → 输出

的小程序。

### 自然显性成果
可以自然形成：

> **《我的数据小报告》**

如果课堂任务已经包含多个变量与计算时使用。

### 知识边界
不使用：
- input；
- 类型转换；
- 分支；
- 循环。

### 下一讲接口
目前数据都是程序员提前写进去的。

下一阶段自然提出：

> **如果程序运行前不知道数据怎么办？**

---

## 阶段测试 T1｜程序、变量与表达式能力测评

### 测试目标
验证：
- 顺序执行；
- 变量跟踪；
- 赋值；
- 运算；
- 复杂表达式；
- 简单完整程序。

### 测试原则
不做语法名词默写。

至少包含：
- 代码阅读；
- Trace；
- 找错；
- 一个短小独立编程任务。

---

# 四、第二阶段｜08～11 输入、转换与顺序建模

## 第08讲 `input()`：程序怎样获得外部数据

### 本讲定位
让程序第一次从“固定数据”进入“运行时数据”。

### 前置知识
- 变量；
- 字符串；
- print；
- 基础程序顺序。

### 核心知识
- `input()`；
- 输入提示；
- 用变量接收输入；
- `input()` 返回字符串；
- 输入与输出的交互关系。

### 课程衔接
07 讲程序的数据全部预先写死。

08 讲解决：

> **程序如何在运行时获得用户数据？**

### 知识点内部衔接
固定值  
→ 数据运行前未知  
→ 用户输入  
→ 变量接收  
→ 输入结果本质是字符串。

### 类题衔接
基础：
- 输入姓名并输出。

同构：
- 输入城市、爱好等文本。

变式：
- 预测 `"10" + "20"` 的结果，为下一讲制造冲突。

### 核心难点
学生容易因为“我输入的是10”而认为得到的就是数值10。

必须建立：

> **键盘输入首先是文本。**

### 能力出口
学生能够正确使用 input 接收并保存文本信息。

### 自然显性成果
可自然形成：

> **《互动问候程序》**

### 知识边界
不提前使用：
- `int()` 解决数值计算，留到下一讲正式建立。

### 下一讲接口
如果输入 `"10"` 和 `"20"`，为什么不能直接得到 30？

---

## 第09讲 类型转换：把输入变成可计算的数据

### 本讲定位
解决输入字符串与数值计算之间的冲突。

### 前置知识
- input；
- 字符串；
- 算术运算；
- 变量。

### 核心知识
- `int()`；
- `float()`；
- `str()`；
- 转换前后的数据变化；
- 常见类型错误；
- 数值输入的标准写法。

### 课程衔接
08 讲留下：
> 输入是字符串。

09 讲自然解决：
> **怎样让输入参与数值计算？**

### 知识点内部衔接
输入字符串  
→ 不能按数值计算  
→ 类型转换  
→ 数值表达式。

### 类题衔接
基础：
- 输入整数后加法。

同构：
- 输入浮点数计算。

变式：
- 判断哪些字符串不能直接转换；
- 找出忘记转换导致的错误。

### 核心难点
学生要理解：
> 转换的是“数据的解释方式”，不是简单加一个函数壳。

### 能力出口
学生能够正确完成：
> 输入 → 转换 → 数值计算。

### 自然显性成果
可自然形成：
> **《单位换算器》**

前提是任务只使用已学知识。

### 知识边界
不引入复杂异常处理。

### 下一讲接口
会输入一个数以后：

> **一个现实问题通常需要多个数据，怎么组织整个求解过程？**

---

## 第10讲 顺序建模：把现实计算翻译成程序

### 本讲定位
第一次把重点从“学语法”转向“拆问题”。

### 前置知识
- input；
- 类型转换；
- 变量；
- 算术表达式。

### 核心知识
- 多个输入；
- 变量命名；
- 明确输入与输出；
- 把公式拆成程序步骤；
- 中间变量；
- 顺序结构。

### 课程衔接
09 讲会把一个输入变成数字。

10 讲升级为：

> **如何把一个完整计算问题翻译成程序？**

### 知识点内部衔接
读任务  
→ 找已知量  
→ 找结果  
→ 设计变量  
→ 输入  
→ 计算  
→ 输出。

### 类题衔接
基础：
- 长方形面积。

同构：
- 周长、平均值、总价。

变式：
- 两步公式；
- 需要中间变量的问题。

### 核心难点
学生容易直接开始敲代码，而不先判断：
- 输入是什么；
- 结果是什么；
- 中间需要什么。

### 能力出口
学生能够把简单现实计算过程拆成明确程序步骤。

### 自然显性成果
根据任务自然命名，例如：
- 《长方形计算器》
- 《时间换算器》

不固定某一个。

### 知识边界
不引入条件判断。

### 下一讲接口
程序已经能算对。

最后还要解决：

> **怎样把结果表达得清楚、完整、像一个真正可用的小程序？**

---

## 第11讲 输出组织与顺序程序综合

### 本讲定位
第二阶段收束。

重点是完整性，而不是继续堆新语法。

### 前置知识
08～10 全部。

### 核心知识
- f-string 实际应用；
- 一级常见基础输出控制（如 `print(..., end="")` 换行控制）；
- 轻量认识多项输出形式；
- 多个结果清晰表达；
- 输入 → 转换 → 计算 → 输出完整流程；
- 综合 Debug。

### 课程衔接
10 讲完成“能算”。

11 讲完成：
> **能完整表达并独立完成。**

下一阶段将第一次改变程序路径。

### 知识点内部衔接
算出结果  
→ 输出组织  
→ 检查完整程序  
→ 独立完成。

### 类题衔接
基础：
- 输出指定格式。

同构：
- 改变量/公式。

变式：
- 找输出格式错误；
- 找类型/计算/输出综合错误。

综合：
- 独立顺序程序。

### 核心难点
学生是否真正能从空白开始组织 4～8 行左右的完整程序。

### 能力出口
学生能够独立完成一个新的简单顺序问题。

### 自然显性成果
自然形成：
> **《生活公式计算器》** 或对应课堂真实任务名称。

### 知识边界
不使用：
- if；
- 比较运算进行控制。

### 下一讲接口
到目前为止：

> 输入不同数据，程序还是执行同一套步骤。

下一阶段提出：

> **不同情况能不能走不同道路？**

---

## 阶段测试 T2｜顺序程序建模能力测评

### 测试目标
验证学生是否真正能完成：
> 自然语言问题 → 输入 → 转换 → 计算 → 输出。

至少包含一个课堂没原样出现的新问题。

---

# 五、第三阶段｜12～16 条件、规则与分支

## 第12讲 比较与布尔：条件为什么会有真假

### 本讲定位
建立分支结构的逻辑地基。

### 前置知识
- 表达式；
- 数值；
- 字符串初识；
- 变量。

### 核心知识
- `>`
- `<`
- `>=`
- `<=`
- `==`
- `!=`
- `True`
- `False`
- 比较表达式也是有结果的表达式。

### 课程衔接
以前表达式主要产生数字或字符串。

现在发现：

> **表达式还可以产生“真/假”。**

这为 if 提供条件。

### 知识点内部衔接
比较两个值  
→ 得到真假  
→ 真/假也属于程序数据。

### 类题衔接
基础：
- 判断比较结果。

同构：
- 变量参与比较。

变式：
- 连续赋值后再比较；
- `=` 与 `==` 辨析。

### 核心难点
- `=` 与 `==`；
- 条件不是一句自然语言，而是会计算出 True/False 的表达式。

### 能力出口
学生能够准确判断常见比较表达式的真假。

### 自然显性成果
**无，不强行包装。**

### 知识边界
不提前讲复杂 `and/or/not`。

### 下一讲接口
既然条件能得到 True/False：

> **程序能不能根据真假决定做不做某件事？**

---

## 第13讲 `if` 与 `if/else`：让程序开始做决定

### 本讲定位
第一次改变程序的固定执行路径。

### 前置知识
- 布尔值；
- 比较表达式；
- 顺序程序。

### 核心知识
- `if`；
- 缩进；
- 单分支；
- `if/else`；
- 双分支；
- 路径跟踪。

### 课程衔接
12 讲得到条件真假。

13 讲解决：
> **真假如何改变执行路径？**

### 知识点内部衔接
条件  
→ 真时执行  
→ 假时跳过  
→ 两种情况都要处理  
→ else。

### 类题衔接
基础：
- 判断能否进入。

同构：
- 正负数；
- 是否达标。

变式：
- 找错缩进；
- 跟踪两种输入走不同路径。

### 核心难点
学生容易认为 if 两边代码都会执行。

必须结合执行路径可视化。

### 能力出口
学生能够写并跟踪单双分支程序。

### 自然显性成果
可自然形成：
> **《奇偶判断器》**
但只有在本讲已经回收 `%` 时使用；如果希望把取模集中到14，则本讲选更普通的阈值判断任务。

### 知识边界
不使用 elif。

### 下一讲接口
已经会判断大小。

下一讲回收一个旧工具 `%`：

> **它还能帮助我们判断哪些规律？**

---

## 第14讲 取模与周期条件：把 `%` 变成判断工具

### 本讲定位
第一次明显展示“旧知识升级身份”。

### 前置知识
- `%` 的余数意义；
- if/else；
- 比较。

### 核心知识
- 奇偶；
- 整除；
- 周期；
- `%` 与条件结合；
- 简单星期/时钟映射思想。

### 课程衔接
05 讲 `%` 只是算余数。

14 讲升级为：
> **判断规律的工具。**

### 知识点内部衔接
余数  
→ 余数等于0  
→ 整除  
→ 奇偶  
→ 余数循环出现  
→ 周期问题。

### 类题衔接
基础：
- 奇偶。

同构：
- 是否整除。

变式：
- 每 k 个单位循环一次；
- 星期/周期映射。

### 核心难点
周期问题不是死记公式，要理解：
> 余数代表“落在周期中的哪个位置”。

### 能力出口
学生能够识别并利用取模解决简单规律判断。

### 自然显性成果
自然适合：
> **《星期推算器》**
或其他实际周期任务。

### 知识边界
不引入循环。

### 下一讲接口
现在程序能处理两种情况。

如果有三种、四种甚至更多情况怎么办？

---

## 第15讲 `elif` 与区间判断：处理多种互斥情况

### 本讲定位
从二分判断升级到规则系统。

### 前置知识
- if/else；
- 比较；
- 取模判断。

### 核心知识
- `elif`；
- 多分支；
- 判断顺序；
- 区间；
- 覆盖与遗漏。

### 课程衔接
13 讲只有两条路。

15 讲解决：
> **多种互斥情况如何组织？**

### 知识点内部衔接
两种情况  
→ 多种情况  
→ elif  
→ 从上到下判断  
→ 区间顺序影响结果。

### 类题衔接
基础：
- 等级判断。

同构：
- 年龄区间、票价。

变式：
- 判断顺序错误；
- 区间重叠；
- 边界值。

### 核心难点
学生会写 elif 后，最容易忽视：
> **顺序本身也是逻辑。**

### 能力出口
学生能够设计正确的多分支区间程序。

### 自然显性成果
自然适合：
> **《成绩等级判定器》**
或真实规则任务。

### 知识边界
不一次性塞入复杂嵌套。

### 下一讲接口
现在每个分支条件通常只有一个条件。

下一讲提出：

> **如果一个规则需要同时判断多个条件怎么办？**

---

## 第16讲 复合条件与分支综合

### 本讲定位
第三阶段收束。

把自然语言中的复杂规则翻译成程序条件。

### 前置知识
12～15 全部。

### 核心知识
- `and`
- `or`
- `not`
- 条件中的简单数字真假：
  - `0 → False`
  - 非0数字 → True
- `not(i % 2)` 与偶数判断的连接；
- 复合规则；
- 必要的简单条件嵌套；
- 分支综合。

### 课程衔接
15 讲解决多种情况。

16 讲解决：
> **单个条件已经不够表达现实规则。**

### 知识点内部衔接
两个条件都要满足  
→ and  
→ 满足任一个  
→ or  
→ 反转真假  
→ not  
→ 复杂规则组织。

### 类题衔接
基础：
- 逻辑表达式真假。

同构：
- 资格判断。

变式：
- 自然语言翻译；
- 找出错误逻辑；
- 边界与逻辑组合。

综合：
- 闰年、月份天数、资格审核等规则题。

### 核心难点
不要把 `and/or/not` 教成单纯符号表。

重点始终是：
> **自然语言规则 → 条件结构。**

### 能力出口
学生能够把一段简单复合规则翻译成分支程序。

### 自然显性成果
根据课堂实际任务自然出现，例如：
> **《闰年判断器》**
> **《资格审核器》**

### 知识边界
不引入循环解决重复问题。

### 下一讲接口
分支解决的是：
> 不同情况走不同道路。

下一阶段进入：
> **同一个过程需要反复发生怎么办？**

---

## 阶段测试 T3｜条件建模能力测评

### 测试目标
必须真正验证：
- 比较；
- 分支路径；
- 边界；
- 多分支；
- 复合条件；
- 自然语言规则翻译。

---

# 六、第四阶段｜17～25 循环、状态与基础算法

## 第17讲 `for` 与 `range`：描述明确范围的重复

### 本讲定位
建立第一个循环模型。

### 前置知识
- 顺序；
- 变量；
- 表达式；
- 分支。

### 核心知识
- `for`；
- `range(stop)`；
- `range(start, stop)`；
- `range(start, stop, step)`；
- 循环变量；
- 循环体；
- 每一轮的值。

### 课程衔接
前面代码只能手工重复。

17 讲解决：
> **明确次数/范围的重复如何表达？**

### 知识点内部衔接
复制同一句  
→ 重复需求  
→ for  
→ 每轮需要不同值  
→ range 与循环变量。

### 类题衔接
基础：
- 输出 0～4。

同构：
- 输出区间。

变式：
- 倒序；
- 步长；
- 预测循环变量序列。

### 核心难点
学生不能只记：
> `for i in range(5)` = 重复5次。

必须真正理解：
> **i 在每轮依次取不同值。**

### 能力出口
学生能够根据明确范围写 for 循环，并预测每轮循环变量。

### 自然显性成果
可自然形成：
> **《规律数字生成器》**
但不强制。

### 知识边界
不提前使用列表展示 range 结果。

### 下一讲接口
循环能产生很多轮结果。

新的问题：

> **前几轮得到的结果怎么留下来？**

---

## 第18讲 累加器与计数器：让循环“记住”结果

### 本讲定位
整套基础课程最关键的状态思维课之一。

### 前置知识
- for；
- range；
- 循环变量；
- 变量更新。

### 核心知识
- 累加器；
- 计数器；
- 初始值；
- 循环外初始化；
- 循环内更新；
- 当前状态；
- 条件累加/计数初步。

### 课程衔接
17 讲每轮“发生了什么”。

18 讲解决：
> **多轮结果怎么汇总成一个答案？**

### 知识点内部衔接
每轮有一个值  
→ 需要保存前面结果  
→ 初始化状态  
→ 每轮更新  
→ 循环结束得到答案。

### 类题衔接
基础：
- 1～n求和。

同构：
- 偶数和；
- 固定条件计数。

变式：
- 为什么初始化不能放循环里；
- 从求和改成计数；
- 累加+计数组合。

### 核心难点
真正理解：
- `total` 在记什么；
- `count` 在记什么；
- 为什么初值通常是0；
- 什么时候更新。

### 能力出口
学生能独立设计简单累加器、计数器。

### 自然显性成果
自然适合：
> **《连续数字求和器》**
> **《小猪存钱罐》**

只选与课堂任务真正贴合的。

### 知识边界
暂不引入 max/min 列表算法。

### 下一讲接口
现在能统计所有数据。

如果只处理“满足某个条件”的数据怎么办？

---

## 第19讲 条件 + 循环：枚举、筛选与统计

### 本讲定位
把分支和循环真正融合。

### 前置知识
- for；
- 累加；
- 计数；
- if；
- `%`。

### 核心知识
- 循环中判断；
- 条件筛选；
- 枚举；
- 条件计数；
- 条件累加；
- 简单查找思想初步。

### 课程衔接
18 讲“每轮都处理”。

19 讲升级：
> **每轮先判断，再决定是否处理。**

### 知识点内部衔接
遍历所有候选  
→ 每个候选做条件判断  
→ 满足则处理  
→ 统计/累加结果。

### 类题衔接
基础：
- 1～100中偶数个数。

同构：
- 倍数；
- 某范围符合条件的数。

变式：
- 多个条件；
- 同时输出+计数；
- 找到符合条件的第一个数，为 break 铺垫。

### 核心难点
学生必须理解：
> 循环负责“一个一个看”，if负责“决定这个要不要”。

### 能力出口
学生能够完成基础枚举与筛选问题。

### 自然显性成果
可自然形成：
> **《条件筛选器》**
若没有合适任务则不命名。

### 知识边界
不使用 break 解决提前结束，留到下一阶段知识。

### 下一讲接口
枚举可以找“符合条件的数”。

接下来：

> **怎样用枚举判断因数与质数？**

---

## 第20讲 因数与质数：把枚举变成算法

### 本讲定位
第一次把“枚举 + 筛选 + 状态”组合成典型基础算法。

### 前置知识
- `%`；
- for；
- if；
- 计数；
- 枚举。

### 核心知识
- 因数；
- 整除；
- 枚举因数；
- 质数；
- 通过因数数量或额外因数判断质数；
- 状态记录。

### 课程衔接
19 讲会枚举所有候选。

20 讲应用到：
> **不知道因数在哪里，就逐个尝试。**

### 知识点内部衔接
整除  
→ 因数  
→ 枚举所有可能除数  
→ 记录因数信息  
→ 质数定义  
→ 判断质数。

### 类题衔接
基础：
- 输出因数。

同构：
- 统计因数个数。

变式：
- 判断质数；
- 找范围内质数；
- 辨析1、2等边界。

### 核心难点
学生不能把“质数模板”当黑盒。

要说得清：
> 程序到底在试什么、记录什么、最后为什么能判断。

### 能力出口
学生能够自己写出基础因数与质数算法，并解释状态含义。

### 自然显性成果
非常自然：
> **《因数查找器》**
> **《质数检测器》**

### 知识边界
不追求高级优化。
不引入 sqrt 优化作为核心。

### 下一讲接口
枚举时常出现：
- 找到答案就不用继续；
- 某一轮不想处理。

如何改变循环执行路径？

---

## 第21讲 `break` 与 `continue`：改变循环执行路径

### 本讲定位
从“循环完整执行”进入“循环内部控制”。

### 前置知识
- for；
- if；
- 枚举；
- 状态变量。

### 核心知识
- `break`；
- `continue`；
- 二者差异；
- 执行流；
- 找到答案提前结束；
- 跳过当前轮；
- 与状态变量配合。

### 课程衔接
20 讲枚举可能一直试到底。

21 讲解决：
> **什么时候可以提前停止？什么时候只跳过一轮？**

### 知识点内部衔接
完整循环  
→ 某轮不处理  
→ continue  
→ 已经找到答案  
→ break。

### 类题衔接
基础：
- 跳过某些数字。

同构：
- 找到第一个倍数后停止。

变式：
- break 放错位置；
- continue 后面的代码是否执行；
- 状态变量更新位置。

### 核心难点
学生需要真正跟执行路径，而不是背：
> break结束，continue跳过。

### 能力出口
学生能够准确判断 break/continue 对执行流的影响。

### 自然显性成果
**无，不强行包装。**

### 知识边界
不正式讲 while，留到下一讲。

### 下一讲接口
for适合范围明确的问题。

如果：

> **根本不知道会执行多少轮，只知道什么时候停呢？**

---

## 第22讲 `while` 与状态模拟：蚂蚁爬树

### 本讲定位
建立条件型循环，并完成一次真正的状态模拟。

### 前置知识
- 变量更新；
- if；
- break；
- 循环状态；
- 累加。

### 核心知识
- `while`；
- 循环条件；
- 初始状态；
- 状态更新；
- 死循环；
- 条件最终要有机会变化；
- 《蚂蚁爬树》状态模型。

### 课程衔接
17～21主要处理：
> 明确范围或明确候选。

22 讲解决：
> **只知道继续条件，不知道执行几轮。**

### 知识点内部衔接
不知道次数  
→ 只知道继续条件  
→ while  
→ 状态必须更新  
→ 否则死循环  
→ 用现实状态模拟理解。

### 核心状态任务：《蚂蚁爬树》
需要维护：
- `day`
- `height`

每轮顺序：
1. 天数增加；
2. 白天爬升；
3. 判断是否到顶；
4. 到顶立即结束；
5. 没到顶才下滑。

### 类题衔接
基础：
- 变量增长直到阈值。

同构：
- 青蛙跳井/蜗牛爬杆。

变式：
- 第一天直接到顶；
- 更新顺序错误；
- 忘记更新导致死循环。

### 核心难点
这一讲真正重点是：
> **事件顺序 + 状态更新 + 停止条件。**

不是单纯背 while 语法。

### 能力出口
学生能够根据“直到某条件成立”的过程写基础 while，并维护状态。

### 自然显性成果
核心命名成果：
> **《蚂蚁爬树》**

### 知识边界
不把本讲再次塞入因数/质数新教学。

### 下一讲接口
while 已经可以让一个数不断变化。

下一讲看一个非常典型的状态变化：

> **怎样把一个整数一位一位拆开？**

---

## 第23讲 数位拆解：逐位处理一个整数

### 本讲定位
将 `%`、`//`、while、状态更新真正连接起来。

### 前置知识
- while；
- `%`；
- `//`；
- 累加/计数；
- 状态更新。

### 核心知识
- `n % 10` 取个位；
- `n // 10` 去掉个位；
- while逐位处理；
- 数位和；
- 数位个数；
- 条件数位统计。

### 课程衔接
22 讲是：
> 状态不断变化直到结束。

23 讲将这一模型用于整数：
> **数字不断变短直到0。**

### 知识点内部衔接
取个位  
→ 处理这一位  
→ 去掉这一位  
→ 下一轮  
→ 数位算法。

### 类题衔接
基础：
- 输出各位；
- 数位和。

同构：
- 数位个数。

变式：
- 统计某数字；
- 奇数位/偶数位统计；
- 条件数位和。

### 核心难点
学生必须把：
> “当前 n”
理解为一个不断变化的状态，而不是原始输入永远不变。

### 能力出口
学生能够独立完成基础数位拆解与统计问题。

### 自然显性成果
自然适合：
> **《数位分析器》**

### 知识边界
暂不使用字符串遍历替代数位算法。

### 下一讲接口
现在会“拆”。

下一讲：

> **拆出来的数字还能不能重新组成一个新数？**

---

## 第24讲 数字重构：逆序、回文与特殊数

### 本讲定位
让学生第一次明显感受：
> 答案可以在循环中被“构造”出来。

### 前置知识
- 数位拆解；
- while；
- 状态更新；
- 累加思想。

### 核心知识
- `digit`；
- `rev = rev * 10 + digit`；
- 逆序数；
- 保存原值；
- 回文判断；
- 典型特殊数问题的状态构造。

### 课程衔接
23 讲从原数中“取信息”。

24 讲升级为：
> **用每轮得到的信息重新构造答案。**

### 知识点内部衔接
拆一位  
→ 新结果向左腾位置  
→ 加入当前位  
→ 完成逆序  
→ 与原值比较  
→ 回文等问题。

### 类题衔接
基础：
- 逆序。

同构：
- 回文。

变式：
- 特殊数；
- 保存原数位置；
- 找错状态更新。

### 核心难点
`rev = rev * 10 + digit` 的状态意义必须讲透。

### 能力出口
学生能够理解并书写基础数字重构算法。

### 自然显性成果
自然适合：
> **《数字魔镜》**
> **《回文检测器》**

### 知识边界
不使用字符串反转替代核心算法。

### 下一讲接口
到目前为止循环大多是一层。

如果：
> **一个重复过程里面还有一个完整重复过程呢？**

---

## 第25讲 嵌套循环与循环综合

### 本讲定位
循环阶段收束。

从一层状态推进到“行 × 列”“外层 × 内层”的二维重复关系。

### 前置知识
17～24主要循环与算法知识。

### 核心知识
- 嵌套循环；
- 外层一次、内层完整执行；
- 行列关系；
- 九九乘法表；
- 图案；
- 简单双重枚举；
- 循环综合问题。

### 课程衔接
单层循环描述“一条重复线”。

25 讲进入：
> **重复里面还有重复。**

这同时为：
- Turtle复杂图形；
- 二维列表；

埋下前置。

### 知识点内部衔接
单循环  
→ 行有很多个  
→ 每一行内部又要重复  
→ 嵌套循环  
→ 行列模型。

### 类题衔接
基础：
- 星号矩形。

同构：
- 九九乘法表。

变式：
- 不同行长度；
- 双重计数；
- 简单双重枚举。

综合：
- 条件 + 嵌套循环 + 累计。

### 核心难点
学生必须理解：
> **外层每走一次，内层会完整重新执行。**

### 能力出口
学生能够跟踪并书写基础嵌套循环，并将多个循环思想组合起来。

### 自然显性成果
非常自然：
> **《九九乘法表》**
或一个规律图案任务。

### 知识边界
不使用二维列表。

### 下一讲接口
循环阶段已经非常抽象。

下一阶段把程序状态直接“画出来”。

---

## 阶段测试 T4｜循环与基础算法能力测评

### 测试重点
显著降低纯语法题比例。

必须验证：
- 重复识别；
- 状态设计；
- 初始值；
- 更新位置；
- 结束条件；
- 枚举；
- while；
- 数位；
- 嵌套循环；
- 独立基础算法。

---

# 七、第五阶段｜26～28 Turtle 状态与视觉程序

## 第26讲 Turtle 状态：位置、方向与基本运动

### 本讲定位
把抽象程序状态变成可见运动状态。

### 前置知识
- 顺序执行；
- 变量/状态；
- 基础循环概念。

### 核心知识
- Turtle 绘图环境与 `import turtle`；
- 海龟默认状态（默认朝向东/右，默认位于画布中心 (0, 0)，默认落笔）；
- 当前坐标与当前方向；
- `forward()`、`backward()`；
- `left()`、`right()`；
- 基础运动组合与轨迹推演。

### 课程衔接
22讲状态是高度、天数。

26讲状态变成：
> **位置与方向。**

### 知识点内部衔接
初始状态  
→ 向前移动  
→ 位置变化  
→ 转向  
→ 方向变化  
→ 连续运动。

### 类题衔接
基础：
- 单步运动。

同构：
- 连续运动预测。

变式：
- 最终位置/方向；
- 找错误转角。

### 核心难点
把“海龟现在在哪、朝哪”当作程序状态，而不是只看画出来的线。

### 能力出口
学生能够根据代码预测基本运动状态。

### 自然显性成果
基础图形可以存在，但本讲不必硬命名作品。

### 知识边界
不使用列表保存坐标。

### 下一讲接口
现在一条边一条边画很麻烦。

> **重复图形能不能交给循环？**

---

## 第27讲 循环绘图：用程序表达几何规律

### 本讲定位
让 `for` 和几何规律发生真正连接。

### 前置知识
- Turtle基本运动；
- for/range；
- 表达式。

### 核心知识
- 循环绘图；
- 正多边形与外角计算 `360 / n`；
- `circle(radius)` 与 `circle(radius, extent, steps)` 步数多边形；
- 画笔外观控制：`pencolor()`、`pensize()`；
- 参数变化与几何重复规律。

### 课程衔接
26 讲手动控制运动。

27 讲解决：
> **重复的边与转角如何用循环表达？**

### 知识点内部衔接
正方形手动画  
→ 四次重复  
→ 循环  
→ n边形  
→ 角度与n关系  
→ 参数化图形。

### 类题衔接
基础：
- 正方形。

同构：
- 三角形、六边形。

变式：
- n边形；
- 改大小/角度；
- circle与循环组合。

### 核心难点
不是背：
> 外角 = 360/n

而是理解：
> 每画一条边后，方向要怎样改变才能围成图形。

### 能力出口
学生能够从图形规律抽象出循环参数。

### 自然显性成果
自然适合：
> **《多边形工坊》**
或对应真实规律图案。

### 知识边界
不使用自定义函数批量封装绘图，函数在35讲正式出现。

### 下一讲接口
前面主要从当前位置一路画。

下一讲需要：
> **跳到指定位置、控制画笔状态、组合多个图形。**

---

## 第28讲 坐标与综合绘图：把规则组合成作品

### 本讲定位
Turtle阶段收束。

让坐标、画笔状态、颜色与循环形成一个完整视觉程序。

### 前置知识
- Turtle基础；
- for；
- 嵌套循环；
- 坐标初步可在本讲正式建立。

### 核心知识
- `penup()`、`pendown()`；
- `goto(x, y)` 与坐标系应用；
- 画笔颜色与填充相关命令：`begin_fill()`、`end_fill()`、`fillcolor()` / `color()`；
- 文本绘制 `write()`；
- 窗口保持 `done()`；
- 综合绘图：已学循环、坐标与几何图形组合作品。

### 课程衔接
27讲从“当前位置”连续绘图。

28讲升级：
> **程序可以主动控制多个位置和绘图状态。**

### 知识点内部衔接
连续画  
→ 需要移动但不留痕  
→ 抬笔  
→ 到指定坐标  
→ 落笔  
→ 多个图形组合  
→ 视觉作品。

### 类题衔接
基础：
- 抬笔移动。

同构：
- 多位置重复绘图。

变式：
- 坐标规律；
- 虚线；
- 颜色/填充组合。

综合：
- 一个有明确程序规律的视觉作品。

### 核心难点
不能为了“作品好看”偷偷使用：
- 列表坐标；
- 元组列表；
- 循环解包；
- 自定义函数。

必须只使用当前已学知识完成。

### 能力出口
学生能够将循环、坐标、变量和图形规则组合成完整 Turtle 程序。

### 自然显性成果
本讲天然适合：
> **一个正式命名的 Turtle 规律作品**

具体名字留给 Gemini 根据最终教学任务设计。

### 知识边界
第33讲前仍不把列表当已学知识。

### 下一讲接口
至此 GESP 一级核心知识基本完成。

下一阶段开始解决：
> **怎样把已经学过的知识稳定用于正式题目和考试？**

---

## 阶段测试 T5｜Turtle程序阅读与规律测评

### 测试重点
OJ可测：
- 最终方向；
- 位置变化；
- 循环次数；
- 图形规律；
- 代码阅读。

实际作品完成情况作为另一类能力证据。

---

# 八、第六阶段｜29～32 GESP 一级专项、模拟与复盘

## 第29讲 GESP 客观题与代码阅读专项

### 本讲定位
把零散知识转换成稳定阅读能力。

### 前置知识
01～28。

### 核心训练
- 表达式、变量跟踪、条件、循环、数位、Turtle 等题型化阅读；
- GESP 计算机基础常识集中复习与题型化辨析：
  - CPU / 处理器的最基础作用；
  - 内存的最基础概念；
  - 输入设备 / 输出设备辨析；
  - 操作系统的基础概念；
  - 文件、文件名、扩展名的基础认识；
  - 编程环境的基本操作；
  - 网络 / 互联网的最基础概念；
  - 常见编程规范与代码符号准确性；
- 典型客观题陷阱与稳定 Trace 习惯。

### 课程衔接
前面主要是“学习知识”。

29讲开始：
> **面对混合题目，先判断它在考什么。**

### 知识点内部衔接
题目  
→ 找核心语法/状态  
→ 跟踪  
→ 排除干扰项  
→ 得出答案。

### 类题衔接
基础题不再按课次单点排列，而是混合出现。

重点：
- 同一知识不同写法；
- 小变式；
- 易错边界。

### 核心难点
避免“凭感觉看代码”。

### 能力出口
学生能够建立稳定代码阅读流程。

### 自然显性成果
**无，不包装。**

### 知识边界
原则上不首次教授新算法。

### 下一讲接口
客观题是“看懂”。

下一讲进入：
> **完整写出一道编程题。**

---

## 第30讲 GESP 编程题完整解题专项

### 本讲定位
训练从读题到可运行程序的完整链条。

### 前置知识
全部一级核心知识。

### 核心训练
- 读题；
- 输入；
- 输出；
- 变量；
- 状态；
- 条件/循环选择；
- 边界；
- 编码；
- 样例自测；
- 输出格式；
- Debug。

### 课程衔接
29讲主要读代码。

30讲解决：
> **从空白开始怎么写完整程序？**

### 知识点内部衔接
读题  
→ 找输入输出  
→ 手动模拟  
→ 找状态  
→ 选结构  
→ 编码  
→ 样例验证  
→ 边界检查。

### 类题衔接
选择几种典型一级题型做“完整解题过程”。

不追求题量，追求完整度。

### 核心难点
很多学生不是不会知识，而是程序完成度低。

### 能力出口
学生能够独立完成一级基础编程题的完整流程。

### 自然显性成果
**无，不强行包装。**

### 知识边界
不临时扩展新算法救题。

### 下一讲接口
现在需要验证：
> **在真实时间压力下能不能稳定完成。**

---

## 第31讲 GESP 一级综合限时实战 / 全真模拟前训练

### 本讲定位
考前综合限时实战与作答节奏训练。按 90 分钟总课时、约 80 分钟有效教学设计。

### 前置知识
01～30。

### 核心任务
- 混合题型限时实战；
- 客观题与编程题作答节奏；
- 时间分配策略（先易后难、留足编程与检查时间）；
- 完整作答训练；
- 考前系统操作检查（环境、命名、文件保存）；
- 模拟前最后校准。

### 课程衔接
30 讲完成单道编程题完整流程。

31 讲解决：
> **面对整套题卷时，如何分配时间和把控作答节奏？**

### 能力出口
学生掌握考试时间分配与检查策略，具备独立应对整卷的心理与节奏准备。

### 自然显性成果
**无，不强行包装。**

### 知识边界
禁止现场新增知识。

### 下一讲接口
完成第 31 讲后，学生参加独立设置的 **阶段测试 T6（120 分钟 GESP Python 一级全真模拟）**，第 32 讲基于 T6 真实结果开展复盘。

---

## 阶段测试 T6｜120 分钟 GESP 一级全真模拟

### 测试性质
- **独立设置的 120 分钟全真模拟**；
- 不占用 38 讲正式课次；
- 直接复用现有 OJ / 刷题系统；
- 按照 GESP 官方现行标准题型、题量与 120 分钟真实时间组织；
- 作为第六阶段最终能力检测。

### 能力出口
产出真实的阶段全真模拟成绩单与错误明细数据，作为第 32 讲复盘的数据来源。

---

## 第32讲 模拟复盘与一级能力收束

### 本讲定位
基于阶段测试 T6（120 分钟全真模拟）真实结果，进行全面归因与能力收束。

### 前置知识
阶段测试 T6 真实作答结果。

### 核心训练
- 错题归因（知识缺口 / Trace错误 / 边界遗漏 / 读题错误 / 输出格式 / 时间分配）；
- 对应知识点针对性同构变式修正；
- 一级能力清单逐项核验；
- 个人一级能力画像构建。

### 课程衔接
T6 得到全真测试“结果”。

32 讲得到：
> **原因与改进方向。**

同时正式完成 01～32 GESP Python 一级核心课程闭环。

### 知识点内部衔接
错题  
→ 分类归因  
→ 回到对应知识  
→ 二次修正与变式强化  
→ 形成个人能力画像。

### 类题衔接
重点做“错题同构变式”，不是盲目刷一套新卷。

### 核心难点
防止复盘变成老师单向念答案。学生必须能说清自己为什么错、下次如何检查。

### 能力出口
完成 GESP 一级核心阶段闭环，建立稳定的个人应考能力画像。

### 自然显性成果
> **个人一级能力画像 / 阶段学习报告**

### 知识边界
不把 33～38 内容伪装成一级考点。

### 下一讲接口
一级阶段完成后，新的问题自然出现：

> **如果真实程序要处理一批数据，怎么办？**

---

# 九、第七阶段｜33～38 Python 基础拓展：数据与代码组织

## 第33讲 列表基础：一次管理一组数据

### 本讲定位
第一次改变学生的数据组织方式。

### 前置知识
- 变量；
- for；
- range；
- 索引概念尚未正式学。

### 核心知识
- 为什么需要列表；
- 创建列表；
- 下标从0开始；
- 访问元素；
- 修改元素；
- `append()`；
- `len()`；
- `for` 遍历列表。

### 课程衔接
以前：
> 一个变量保存一个数据。

33讲提出：
> **30个数据难道定义30个变量？**

### 知识点内部衔接
多个变量麻烦  
→ 一组数据  
→ 列表  
→ 找某一个  
→ 下标  
→ 修改  
→ 新增  
→ 长度  
→ 遍历整组。

### 类题衔接
基础：
- 下标预测。

同构：
- 修改/append。

变式：
- `len(nums)-1`；
- 找最后一个位置。

综合：
- 建立、修改、遍历一个小型数据清单。

### 核心难点
下标从0开始必须真正理解。

本讲要安排大量索引预测。

### 能力出口
学生能够创建、访问、修改、增加并遍历一组数据。

### 自然显性成果
自然适合：
> **《我的数据清单》**

情境可由 Gemini 在成绩、背包、运动数据等中选择。

### 知识边界
暂不正式教授：
- insert；
- remove；
- pop；
- sort；
- reverse；
- 切片；
- 列表推导式；
- enumerate。

### 下一讲接口
现在能“把一批数据装起来”。

下一讲真正解决：
> **循环怎样处理这一批数据？**

---

## 第34讲 列表算法：让循环处理真实数据

### 本讲定位
这是列表模块真正重要的一讲。

将前面算法思想迁移到一批真实数据。

### 前置知识
- 列表基础；
- for；
- 累加；
- 计数；
- 条件；
- range。

### 核心知识
- 列表求和；
- 计数；
- 最大值；
- 最小值；
- 查找；
- 查找下标；
- `for i in range(len(nums))`；
- 在自己理解算法后轻量认识 `sum/max/min`。

### 课程衔接
33讲只是：
> 有一批数据。

34讲解决：
> **怎样从一批数据中产生答案？**

### 知识点内部衔接
遍历  
→ 每个元素参与状态更新  
→ 累加/计数  
→ 当前最大/最小  
→ 查找  
→ 需要位置时改用下标遍历。

### 类题衔接
基础：
- 求和。

同构：
- 计数。

变式：
- 最大/最小；
- 查找；
- 第一个位置/最后位置。

综合：
- 一组成绩分析。

### 核心难点
最大值算法的本质：
> `max_num` 维护的是“目前见过的最好答案”。

### 能力出口
学生能够自己写基础列表算法，而不是只调用内置函数。

### 自然显性成果
核心成果：
> **《成绩分析器》**

### 知识边界
暂不使用 enumerate 作为主解法。
暂不进入二维列表。

### 下一讲接口
现在程序已经越来越长，而且一些代码会反复出现。

> **重复功能能不能被组织起来？**

---

## 第35讲 函数基础：给一段代码起名字

### 本讲定位
从数据组织进入代码组织。

### 前置知识
- 顺序；
- 条件；
- 循环；
- Turtle；
- 列表。

### 核心知识
- 为什么需要函数；
- `def`；
- 函数定义；
- 函数调用；
- 主程序 → 函数 → 回到原位置；
- 一个参数；
- 多个参数。

### 课程衔接
此前所有程序基本从第一行写到最后一行。

35讲提出：
> **一段功能反复出现怎么办？**

### 知识点内部衔接
重复代码  
→ 打包  
→ 命名  
→ 调用  
→ 每次需要不同数据  
→ 参数。

### 类题衔接
基础：
- 无参数函数。

同构：
- 一个参数。

变式：
- 多参数；
- 预测调用顺序；
- 参数位置错误。

### 核心难点
函数不是“神秘语法”。

真正核心是：
> **代码执行会暂时进入函数，然后回到调用处。**

### 能力出口
学生能够定义并调用带参数的简单函数。

### 自然显性成果
可以自然形成：
> **《绘图小工具》**
或多个功能函数，但不要求本讲直接做“工具箱”。

### 知识边界
暂不深入：
- 局部/全局变量；
- 默认参数；
- 关键字参数；
- 可变参数；
- lambda。

### 下一讲接口
现在函数可以“做事情”。

新的问题：

> **函数算出来的答案，怎么交回来继续用？**

---

## 第36讲 `return` 与算法封装：让结果带回来

### 本讲定位
完成函数从“执行动作”到“产生结果”的关键跃迁。

### 前置知识
- 函数定义与调用；
- 参数；
- 旧算法；
- 列表基础算法。

### 核心知识
- `return`；
- 返回值；
- `print` 与 `return`；
- 接收函数结果；
- 返回结果继续参与计算；
- 旧算法封装。

### 课程衔接
35讲函数能做事。

36讲解决：
> **函数能不能把答案交回主程序？**

### 知识点内部衔接
函数内部计算  
→ print只能展示  
→ 主程序拿不到结果  
→ return  
→ 接收结果  
→ 继续计算。

### 类题衔接
基础：
- add(a,b)。

同构：
- 面积函数。

变式：
- is_even；
- get_max；
- 列表求和函数。

综合：
- 多个小函数组成工具箱。

### 核心难点
必须真正区分：
> **显示结果** 和 **返回结果**。

### 能力出口
学生能够写带 return 的函数，并将旧算法封装复用。

### 自然显性成果
核心成果：
> **《我的 Python 工具箱》**

### 知识边界
不进入复杂作用域理论。

### 下一讲接口
已经会：
> 一批数据。

下一步：
> **如果数据天然有行和列呢？**

---

## 第37讲 二维列表：数据也可以有行和列

### 本讲定位
把一维数据组织升级成表格型数据。

### 前置知识
- 列表；
- 下标；
- 嵌套循环；
- 函数。

### 核心知识
- 列表里面装列表；
- 行；
- 列；
- `scores[0]`；
- `scores[0][1]`；
- 修改二维元素；
- 外层遍历行；
- 嵌套循环遍历全部元素。

### 课程衔接
33讲解决“一批数据”。

37讲提出：
> **如果一批数据内部还有结构怎么办？**

### 知识点内部衔接
列表  
→ 列表里再放列表  
→ 先找行  
→ 再找列  
→ 双下标  
→ 嵌套遍历。

### 类题衔接
基础：
- 双下标预测。

同构：
- 修改指定格子。

变式：
- 遍历一行；
- 遍历全部；
- 统计满足条件元素。

### 核心难点
建立：
> **先找第几行，再找这一行第几列。**

必须用表格/九宫格等可视化支持。

### 能力出口
学生能够访问、修改并遍历基础二维列表。

### 自然显性成果
可以自然形成：
> **《座位表》**
> **《九宫格数据表》**
但不强制。

### 知识边界
不引入：
- NumPy；
- 矩阵运算；
- DFS/BFS；
- 复杂邻接问题。

### 下一讲接口
现在学生会单独使用：
- 列表；
- 函数；
- 二维列表。

最后一讲要回答：

> **能不能把这些知识真正组合起来？**

---

## 第38讲 二维数据综合：把知识组织成完整程序

### 本讲定位
整套38讲课程最终收束。

不是大型项目课，而是完整问题解决课。

### 前置知识
01～37核心能力。

### 核心知识
- 二维数据；
- 条件；
- 循环；
- 列表；
- 函数；
- return；
- 每行处理；
- 全表统计；
- 综合数据分析；
- 程序组织。

### 课程衔接
37讲是“会二维列表”。

38讲要求：
> **真正用它解决一个完整问题。**

### 知识点内部衔接
二维数据  
→ 每行是一组数据  
→ 用函数处理一行  
→ 外层遍历所有行  
→ 条件统计  
→ 输出完整分析结果。

### 类题衔接
基础：
- 每行求和。

同构：
- 每行最大值。

变式：
- 全表计数；
- 查找满足条件的数据。

综合：
- 多学生 × 多科成绩分析。

### 核心难点
不是代码长度。

重点是：
> **学生能否把多个知识组织起来，而不是老师替他搭好所有骨架。**

### 能力出口
学生能够独立完成一个由：
> 列表 + 条件 + 循环 + 函数 + 二维列表

组成的中小型完整程序。

### 自然显性成果
核心成果：
> **《班级成绩分析》**

若后续单讲课研发现更合适的真实二维数据任务，可更换具体情境，但不能改变能力目标。

### 知识边界
本课程到此为止，不为了“完整项目”继续引入：
- 字典；
- 文件；
- 类；
- 第三方库；
- 数据库。

这些属于后续课程。

### 下一阶段接口
无。

本讲结束后进入：
> 阶段测试 T7 + 课程总复盘。

---

## 阶段测试 T7｜Python基础拓展综合测评

### 测试重点
不以列表方法记忆为主。

重点验证：
- 列表索引；
- 列表算法；
- 函数调用；
- return；
- 二维索引；
- 嵌套遍历；
- 综合程序组织。

---

# 十、38讲纵向知识回收检查

## 1. 变量 / 状态主线

> 03 变量保存  
> → 04 状态变化  
> → 10 中间变量  
> → 13 分支中的状态  
> → 18 累加/计数  
> → 20 算法状态  
> → 22 状态模拟  
> → 23/24 数位状态  
> → 34 当前最值  
> → 36 函数结果  
> → 38 综合数据状态

---

## 2. `%` 主线

> 05 余数  
> → 14 奇偶/周期  
> → 19 条件筛选  
> → 20 因数/质数  
> → 23 数位拆解

---

## 3. 循环主线

> 17 for/range  
> → 18 累加/计数  
> → 19 筛选  
> → 20 枚举算法  
> → 21 执行流控制  
> → 22 while  
> → 23/24 数位  
> → 25 嵌套  
> → 27/28 Turtle  
> → 34 列表  
> → 37/38 二维数据

---

## 4. 嵌套循环主线

> 25 行列与九九乘法表  
> → 28 组合规律绘图  
> → 37 二维列表遍历  
> → 38 二维数据综合

---

## 5. 问题解决主线

> 07 固定数据小程序  
> → 10 顺序建模  
> → 16 规则建模  
> → 19 枚举筛选  
> → 22 状态模拟  
> → 24 数字构造  
> → 25 循环综合  
> → 30 完整编程题  
> → 34 数据算法  
> → 36 功能封装  
> → 38 综合程序

---

# 十一、阶段测试节点

阶段测试不计入38讲正式课次。

| 测试 | 时间点 | 主要验证 |
|---|---|---|
| T1 | 07讲后 | 程序执行、变量跟踪、表达式 |
| T2 | 11讲后 | 输入、转换、顺序问题建模 |
| T3 | 16讲后 | 条件翻译、分支、边界、复合规则 |
| T4 | 25讲后 | 循环状态、累加计数、枚举、while、数位、嵌套 |
| T5 | 28讲后 | Turtle程序阅读、方向、位置、循环规律 |
| T6 | 第31讲后 | 120 分钟 GESP Python 一级全真模拟（独立组织，复用 OJ / 刷题系统） |
| T7 | 38讲后 | 列表、函数、二维数据与综合组织 |

阶段测试统一复用现有 OJ / 刷题系统。

测试是：
> **阶段能力出口的验证**

不是新增课程知识。

---

# 十二、当前课次地图结论

本版38讲切分满足以下原则：

1. Python本身和程序思维始终是主线；
2. 变量从第一次出现就承担“状态”意义；
3. 循环阶段以“状态维护”为核心，而不是循环语法堆叠；
4. 累加、计数、枚举、因数质数、数位等算法形成连续知识链；
5. Turtle回收循环和状态，不成为独立“画画课程”；
6. 29～32按能力专项、正式模拟、复盘组织，而不是按题源年份组织；
7. 列表、函数、二维列表在一级之后自然进入；
8. 旧知识在后续不断被回收与升级；
9. 自然成果可以命名，但大量纯思维课明确不强行包装；
10. 阶段测试直接验证能力出口，不反向决定课程。

---

# 十三、下一步研发任务

本地图验收后，不马上让 Gemini 批量生成38讲课件。

下一步进入：

# 《单讲课研规格》

从第01讲开始逐讲完成。

每一讲进一步明确：

- 本讲为什么现在教；
- 学生从哪个旧认知出发；
- 新知识需求怎样自然产生；
- 知识点之间的精确衔接；
- 哪些核心问题必须真正想明白；
- 基础 → 同构 → 变式 → 综合怎样递进；
- 哪些旧知识必须回收；
- 最终要做到什么；
- 是否存在自然显性成果；
- 给下一讲留下什么认知接口。

然后由 Gemini 根据：
> 总纲 + 七阶段依赖 + 38讲地图 + 当前讲课研规格 + 互动课件 Skill

自主完成教学设计与课件实现。

---

# 最终原则

> **课次是知识生长的切片，不是知识点清单。**

> **每一讲都必须解释清楚：为什么现在学它，它从哪里来，又要把学生送到哪里去。**
